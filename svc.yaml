apiVersion: v1
kind: Service
metadata:
  labels:
    app: jigme-deploy
  name: jigme
  #namespace: pet
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: jigme-deploy
  type: NodePort
